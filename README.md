# API em NODEJS - Pós Unipar
Trabalho de conclusão da materia de NodeJs - pós unipar - 2022

## Development server

Run `npm install` download project dependencies.

Run `npm start`start the server api localhost on port 3000.
